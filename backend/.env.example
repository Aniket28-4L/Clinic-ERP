NODE_ENV=development
PORT=5000

# Local MongoDB example:
# MONGODB_URI=mongodb://127.0.0.1:27017/clinic_erp
#
# MongoDB Atlas (SRV) example (put the real credentials in your local `.env`, not in source control):
MONGODB_URI=mongodb+srv://aniketpandey4723k_clinic_user:HAgV79YWNnYrG6gc@clinic-erp.dus2qea.mongodb.net/

JWT_ACCESS_SECRET=replace_me_with_a_very_long_strong_secret_value_32_chars_min
JWT_ACCESS_EXPIRES_IN=15m

# For later: refresh token rotation, email, etc.
